release: v0.2.7

fix: revert WebSocket headers that broke ingress proxy (#2)

The Upgrade/Connection headers added in v0.2.5 caused
"Cannot write to closing transport" errors in the HA Supervisor
aiohttp proxy. Dozzle uses SSE, not WebSocket - reverted to
Connection '' (keep-alive). Accept-Encoding "" fix preserved.
