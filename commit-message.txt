release: v0.2.5

fix: add WebSocket upgrade support to ingress proxy (#2)

- Dozzle log filter/search requires WebSocket, which the ingress
  nginx proxy was silently dropping (missing Upgrade headers).
- Move WebSocket map to nginx.conf (shared by ingress + direct access).
- Remove duplicate map from direct_access.conf template.
