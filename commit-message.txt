release: v0.1.4

- fix(nginx): move temp path directives inside http{} block (were at main context — fatal error)
- feat(logs): BUILD_VERSION + DOZZLE_VERSION exposed as ENV vars, cleaner startup banner
